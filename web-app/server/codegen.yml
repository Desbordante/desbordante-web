overwrite: true
schema: "src/graphql/schema/**/*.ts"
documents: null
generates:
  src/graphql/types/types.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
